export interface IContactSlide {
  url: string;
  name: string;
  role: string;
  mobile: string;
  id: number;
}

export interface IContactsBlock {
  address: string;
  room: string;
  number: string;
}
